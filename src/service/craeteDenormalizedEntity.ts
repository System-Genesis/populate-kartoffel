import denormalizedEntitySchema from "../config/schemes/denormalizedEntitySchema";
import { DigitalIdentity, Entity, OrganizationGroup, Role, DenormalizedEntity } from "../config/types";

export default async (entity :Entity, digitalIdentities :DigitalIdentity[], organizationGroup :OrganizationGroup, role: Role) => {
    const denormalizedEntity = entity.
};
